// import React from "react"; - it's no longer required in the latest version(17+) of React because it uses a new JSX Transform.

const ProjectsPage = () => {
    return(
        <>
            <h1>Projects</h1>
        </>
    )
};

export default ProjectsPage;